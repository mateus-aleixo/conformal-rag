# data — corpus is fetched, never committed
data/raw/
data/index.db
# fitted gate: a generated artifact, like conformal-seg's runs/.
# Calibration numbers belong in docs/results.md, not as a committed blob.
data/gate.json
*.pdf

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
uv.lock

# traces and local results
traces/
*.jsonl.tmp

# editors / os
.vscode/
.idea/
Thumbs.db
.DS_Store
